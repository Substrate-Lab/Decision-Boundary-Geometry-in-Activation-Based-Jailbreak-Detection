# Local Claude context / private notes — never commit
CLAUDE.local.md

# Python virtual environment and caches — never commit
.venv/
venv/
__pycache__/
*.pyc

# Generated experiment outputs (activations, PNS scores, figures, HF cache)
# Raw activation arrays only (4.3 GB) -- too large for git.
# Derived results (CSVs, pns_*.npy, sites_*.npz, analytics) and figures ARE tracked,
# and are regenerable from the activations via experiment1 step2/step3.
experiment1/data/**/activations*.npy
experiment2/data/**/activations*.npy
experiment3/data/**/activations*.npy
dataset/data/

# Local machine-specific runner (absolute model path)
experiment1/run_local.ps1

# macOS Finder metadata
.DS_Store
